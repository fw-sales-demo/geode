options:
  organization: centaurus
  setExitCode: false

manifests:
  yaml:
    - ./busybox.yaml
    - ./nginx.yaml
    - ./custom
    - /app/repository/geode

tfsec:
    enabled: true
    customChecksDirectory: /app/repository/geode/checks