options:
  organization: centaurus
  setExitCode: false

manifests:
  yaml:
    - ./busybox.yaml
    - ./nginx.yaml
    - .

tfsec:
    enabled: true
    customChecksDirectory: /app/repository/geode/checks